<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="turbo-visit-control" content="reload" />
    <title>Styleguide - The Walking Dog</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Polices Google Fonts - Bitter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bitter:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- CSS du Styleguide -->
    <%= stylesheet_link_tag "styleguide/styleguide", media: "all" %>

    <!-- JavaScript importmap -->
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>

    <!-- JavaScript du Styleguide -->
    <script type="module">
      // Test de chargement
      // console.log("📦 Script chargé");

      import("styleguide/index")
        .then(() => console.log("✅ Module styleguide chargé"))
        .catch(err => console.error("❌ Erreur chargement module:", err));
    </script>
  </body>
</html>
