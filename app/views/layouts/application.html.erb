<!DOCTYPE html>
<html>
  <head>
    <title>TheWalkingDog</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "mapbox-gl", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="<%= 'fullscreen-map' if controller_name == 'walkings' && action_name == 'show' %>">
    <%= yield %>
    <%= render "layouts/navbar" unless controller_name == "walkings" && action_name == "show" %>
  </body>
</html>
